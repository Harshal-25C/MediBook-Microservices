package com.medibook.auth.config;

/**
 * CORS is intentionally handled only by api-gateway.
 *
 * This placeholder keeps older tests/source references valid without
 * registering a service-level CORS filter that would duplicate browser
 * response headers.
 */
public class CorsConfig {
}
